# --- Supabase -----------------------------------------------------------------
# Project URL and the publishable/anon key: Supabase dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://pscntfoanosvowllobjv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Bypasses RLS — never expose this to the browser and never commit it.
SUPABASE_SERVICE_ROLE_KEY=

# --- App ----------------------------------------------------------------------
# Used to build absolute URLs for auth redirects and courier tracking links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
